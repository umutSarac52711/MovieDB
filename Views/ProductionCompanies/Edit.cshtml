@model MovieDB.Models.ProductionCompanyViewModel

@{
    ViewBag.Title = "Edit Production Company";
    Layout = "_Layout";
}

<h2>Edit Production Company: @Model.Name</h2>

<form method="post" asp-action="Edit">
    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
    <input type="hidden" asp-for="Company_ID" />

    <div class="mt-3">
        <div class="mb-3">
            <label asp-for="Name" class="form-label"></label>
            <input asp-for="Name" class="form-control" />
            <span asp-validation-for="Name" class="text-danger"></span>
        </div>

        <div class="mb-3">
            <label asp-for="Country" class="form-label"></label>
            <input asp-for="Country" class="form-control" />
            <span asp-validation-for="Country" class="text-danger"></span>
        </div>

        <div class="mb-3">
            <label asp-for="Founded_Year" class="form-label">Founded Year</label>
            <input asp-for="Founded_Year" type="number" class="form-control" />
            <span asp-validation-for="Founded_Year" class="text-danger"></span>
        </div>

        <button type="submit" class="btn btn-primary mt-3">Save Changes</button>
        <a asp-action="List" asp-controller="ProductionCompanies" class="btn btn-secondary mt-3">Cancel</a>
    </div>
</form>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
